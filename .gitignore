# Build output
build/
build-*/
*.o
*.obj
*.exe

# Python caches
__pycache__/
*.pyc

# Logs, PGNs, datasets, numpy caches, the rules PDF
*.pgn
*.log
*.err
*.npz
*.pdf
data/
syzygy/
syzygy_test/

# Generated training data, sample dumps and net binaries (regenerate from source).
# The exceptions below are the only such files the build / unit tests need.
*.txt
*.bin
!openings.bin
!engine-adapter.sh
!nets/king_int8_174m.bin
!trainer/s_int8_174m.txt

# Large public datasets — fetch separately, don't commit
tools/quiet-labeled.epd
tools/*.epd.gz
tools/baseline/king_v*

# Accidentally-generated in-source CMake artifacts (the build uses a fresh build/)
/Makefile
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
CTestTestfile.cmake

# Claude Code workspace artifacts — never part of the project repo
.claude/
